providers = ["python"]

[phases.setup]
# 📦 Native system packages fetch Stockfish and Fairy Stockfish directly via APT
nixPkgs = ["python3"]
aptPkgs = ["stockfish", "fairy-stockfish"]

[phases.build]
cmds = [
  "pip install -r requirements.txt"
]
